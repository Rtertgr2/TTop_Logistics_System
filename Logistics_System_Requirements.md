# เอกสารสรุปความต้องการของระบบ (Requirements)
# โครงการ: ระบบจัดคิวรถและเส้นทางจัดส่งอัตโนมัติ (Automated Route Planning & Dispatch System)

## 1. กระบวนการทำงานปัจจุบัน (As-Is Process)
* **การจัดเส้นทางและการแบ่งโซน:** ปัจจุบันใช้พนักงาน (คน) ในการจัดเส้นทาง แบ่งโซนจัดส่ง และทำข้อมูลผ่านไฟล์ Excel
* **การจัดสรรรถจัดส่ง:** มีรถให้บริการจำนวน 4 คัน ปัจจุบันระบุและแบ่งตาม "ชื่อคนขับ" 
  * *ข้อกำหนดใหม่:* ระบบควรปรับไปใช้การระบุ **"ทะเบียนรถ"** แทนชื่อคนขับเพื่อความชัดเจนในการบริหารจัดการ
* **เงื่อนไขการจัดสรร:** แบ่งงานและจัดรถตามเงื่อนไข **"น้ำหนักของสินค้า"**
* **การส่งมอบเส้นทาง:** เมื่อจัดโซนลงใน Excel เสร็จสิ้น พนักงานจะต้องทำการสร้างลิงก์ (Link) Google Maps แบบ Manual เพื่อส่งให้คนขับตามแต่ละ Job

---

## 2. กระบวนการทำงานของระบบใหม่ที่ต้องการ (To-Be Process)

### ขั้นตอนที่ 1: การรับและอ่านไฟล์ (Data Extraction)
* ระบบเริ่มต้นการทำงานเมื่อมีผู้ใช้อัปโหลดไฟล์ OP (อยู่ในรูปแบบ PDF)
* ระบบทำการสกัด (Extract) ข้อมูลดิบจาก PDF เพื่อให้ระบบนำไปใช้งานต่อ โดยข้อมูลที่ต้องดึงออกมาได้แก่:
  1. รายชื่อลูกค้า
  2. สถานที่จัดส่ง
  3. น้ำหนักของสินค้า

### ขั้นตอนที่ 2: การทำความสะอาดและเตรียมข้อมูล (Data Preparation)
* นำข้อมูลที่สกัดได้มาตรวจสอบความถูกต้อง (Data Validation) เช่น ตรวจสอบว่าพิกัด Latitude / Longitude ครบถ้วนหรือไม่
* ทำการแปลงข้อมูล (Transform) ให้อยู่ในรูปแบบโครงสร้างมาตรฐานที่ระบบสามารถนำไปประมวลผลต่อได้ เช่น รูปแบบ **JSON** หรือบันทึกลงใน **Database**

### ขั้นตอนที่ 3: การคำนวณระยะทางบนแผนที่ (Distance & Time Matrix)
* ระบบเชื่อมต่อกับ Mapping API เช่น **Google Maps API** หรือ **OSRM**
* นำข้อมูลสถานที่จัดส่งมาสร้างตารางเมทริกซ์เพื่อคำนวณหาระยะทางและเวลาที่ใช้เดินทางจริง ระหว่าง "ทุกจุดจอด" (เช่น จาก คลังสินค้า -> จุด A, และจาก จุด A -> จุด B)

### ขั้นตอนที่ 4: การประมวลผลจัดคิวรถและเส้นทาง (Route Optimization / VRP)
* นำข้อมูลทั้งหมดเข้าสู่สมองกลหลักของระบบเพื่อรันอัลกอริทึมแก้ปัญหาการจัดเส้นทางยานพาหนะ (Vehicle Routing Problem - VRP)
* ปัจจัยที่ใช้ในการประมวลผล:
  * ข้อมูลออเดอร์ (สถานที่, น้ำหนัก)
  * ข้อจำกัดของรถ (เช่น ความจุสูงสุด/เงื่อนไขน้ำหนัก)
  * ข้อมูลระยะทางและเวลาจากแผนที่
* **เป้าหมายของการประมวลผล (Expected Outputs จากโมเดล):**
  1. ควรใช้รถกี่คันในการจัดส่งรอบนี้?
  2. รถคันไหนต้องวิ่งไปส่งให้ลูกค้ารายใดบ้าง?
  3. ลำดับการวิ่งของแต่ละคันเป็นอย่างไร (จุดที่ 1, 2, 3...) เพื่อให้ประหยัดเวลาและน้ำมันมากที่สุด

### ขั้นตอนที่ 5: การออกผลลัพธ์การจัดส่ง (Output Generation)
* เมื่อโมเดลประมวลผลเสร็จสิ้น ระบบจะสร้างสรุปแผนงาน (Route Plan) สำหรับนำไปปฏิบัติจริง
* ผลลัพธ์ต้องประกอบด้วยข้อมูลต่อไปนี้:
  * ลำดับจุดส่งของสำหรับรถแต่ละคัน
  * รหัสรถ / ทะเบียนรถ
  * ที่อยู่จัดส่ง
  * โซนจัดส่ง
  * น้ำหนักรวมของรถแต่ละคัน
  * ลิงก์เส้นทาง (Google Maps Link) สำหรับการเดินทาง
* **การส่งต่อข้อมูล:** ระบบส่งออกผลลัพธ์ทั้งหมดไปยัง **Email** หรือ **Platform อื่นๆ** ที่กำหนดไว้อัตโนมัติ เพื่อให้คนขับหรือผู้ที่เกี่ยวข้องรับไปดำเนินการต่อ
